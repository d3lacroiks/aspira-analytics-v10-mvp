// preload.js — puente de seguridad entre Electron y la app web
// Por ahora no expone APIs adicionales, solo actúa como capa de seguridad
window.addEventListener("DOMContentLoaded", () => {
  console.log("ASPIRA Analytics — Electron preload OK");
});
